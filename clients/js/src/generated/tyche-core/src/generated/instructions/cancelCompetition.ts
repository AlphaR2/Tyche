/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TYCHE_CORE_PROGRAM_ADDRESS } from "../programs";

export const CANCEL_COMPETITION_DISCRIMINATOR = new Uint8Array([5]);

export function getCancelCompetitionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 1).encode(
    CANCEL_COMPETITION_DISCRIMINATOR,
  );
}

export type CancelCompetitionInstruction<
  TProgram extends string = typeof TYCHE_CORE_PROGRAM_ADDRESS,
  TAccountCompetition extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountPermission extends string | AccountMeta<string> = string,
  TAccountMagicContext extends string | AccountMeta<string> = string,
  TAccountMagicProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCompetition extends string
        ? WritableAccount<TAccountCompetition>
        : TAccountCompetition,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountPermission extends string
        ? WritableAccount<TAccountPermission>
        : TAccountPermission,
      TAccountMagicContext extends string
        ? WritableAccount<TAccountMagicContext>
        : TAccountMagicContext,
      TAccountMagicProgram extends string
        ? ReadonlyAccount<TAccountMagicProgram>
        : TAccountMagicProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CancelCompetitionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CancelCompetitionInstructionDataArgs = {};

export function getCancelCompetitionInstructionDataEncoder(): FixedSizeEncoder<CancelCompetitionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 1)]]),
    (value) => ({ ...value, discriminator: CANCEL_COMPETITION_DISCRIMINATOR }),
  );
}

export function getCancelCompetitionInstructionDataDecoder(): FixedSizeDecoder<CancelCompetitionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 1)],
  ]);
}

export function getCancelCompetitionInstructionDataCodec(): FixedSizeCodec<
  CancelCompetitionInstructionDataArgs,
  CancelCompetitionInstructionData
> {
  return combineCodec(
    getCancelCompetitionInstructionDataEncoder(),
    getCancelCompetitionInstructionDataDecoder(),
  );
}

export type CancelCompetitionInput<
  TAccountCompetition extends string = string,
  TAccountAuthority extends string = string,
  TAccountPermission extends string = string,
  TAccountMagicContext extends string = string,
  TAccountMagicProgram extends string = string,
> = {
  /** CompetitionState PDA */
  competition: Address<TAccountCompetition>;
  /** Must match state.authority */
  authority: TransactionSigner<TAccountAuthority>;
  /** MagicBlock ACL permission PDA — undelegated on Active path */
  permission: Address<TAccountPermission>;
  /** MagicBlock context account — used on Active path only */
  magicContext: Address<TAccountMagicContext>;
  /** MagicBlock program — used on Active path only */
  magicProgram: Address<TAccountMagicProgram>;
};

export function getCancelCompetitionInstruction<
  TAccountCompetition extends string,
  TAccountAuthority extends string,
  TAccountPermission extends string,
  TAccountMagicContext extends string,
  TAccountMagicProgram extends string,
  TProgramAddress extends Address = typeof TYCHE_CORE_PROGRAM_ADDRESS,
>(
  input: CancelCompetitionInput<
    TAccountCompetition,
    TAccountAuthority,
    TAccountPermission,
    TAccountMagicContext,
    TAccountMagicProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CancelCompetitionInstruction<
  TProgramAddress,
  TAccountCompetition,
  TAccountAuthority,
  TAccountPermission,
  TAccountMagicContext,
  TAccountMagicProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? TYCHE_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    competition: { value: input.competition ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    permission: { value: input.permission ?? null, isWritable: true },
    magicContext: { value: input.magicContext ?? null, isWritable: true },
    magicProgram: { value: input.magicProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("competition", accounts.competition),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("permission", accounts.permission),
      getAccountMeta("magicContext", accounts.magicContext),
      getAccountMeta("magicProgram", accounts.magicProgram),
    ],
    data: getCancelCompetitionInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelCompetitionInstruction<
    TProgramAddress,
    TAccountCompetition,
    TAccountAuthority,
    TAccountPermission,
    TAccountMagicContext,
    TAccountMagicProgram
  >);
}

export type ParsedCancelCompetitionInstruction<
  TProgram extends string = typeof TYCHE_CORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** CompetitionState PDA */
    competition: TAccountMetas[0];
    /** Must match state.authority */
    authority: TAccountMetas[1];
    /** MagicBlock ACL permission PDA — undelegated on Active path */
    permission: TAccountMetas[2];
    /** MagicBlock context account — used on Active path only */
    magicContext: TAccountMetas[3];
    /** MagicBlock program — used on Active path only */
    magicProgram: TAccountMetas[4];
  };
  data: CancelCompetitionInstructionData;
};

export function parseCancelCompetitionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelCompetitionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      competition: getNextAccount(),
      authority: getNextAccount(),
      permission: getNextAccount(),
      magicContext: getNextAccount(),
      magicProgram: getNextAccount(),
    },
    data: getCancelCompetitionInstructionDataDecoder().decode(instruction.data),
  };
}
