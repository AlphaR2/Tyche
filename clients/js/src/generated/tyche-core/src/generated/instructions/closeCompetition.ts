/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TYCHE_CORE_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_COMPETITION_DISCRIMINATOR = new Uint8Array([3]);

export function getCloseCompetitionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 1).encode(
    CLOSE_COMPETITION_DISCRIMINATOR,
  );
}

export type CloseCompetitionInstruction<
  TProgram extends string = typeof TYCHE_CORE_PROGRAM_ADDRESS,
  TAccountCompetition extends string | AccountMeta<string> = string,
  TAccountCrank extends string | AccountMeta<string> = string,
  TAccountPermission extends string | AccountMeta<string> = string,
  TAccountMagicContext extends string | AccountMeta<string> = string,
  TAccountMagicProgram extends string | AccountMeta<string> = string,
  TAccountProtocolConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCompetition extends string
        ? WritableAccount<TAccountCompetition>
        : TAccountCompetition,
      TAccountCrank extends string
        ? ReadonlySignerAccount<TAccountCrank> &
            AccountSignerMeta<TAccountCrank>
        : TAccountCrank,
      TAccountPermission extends string
        ? WritableAccount<TAccountPermission>
        : TAccountPermission,
      TAccountMagicContext extends string
        ? WritableAccount<TAccountMagicContext>
        : TAccountMagicContext,
      TAccountMagicProgram extends string
        ? ReadonlyAccount<TAccountMagicProgram>
        : TAccountMagicProgram,
      TAccountProtocolConfig extends string
        ? ReadonlyAccount<TAccountProtocolConfig>
        : TAccountProtocolConfig,
      ...TRemainingAccounts,
    ]
  >;

export type CloseCompetitionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseCompetitionInstructionDataArgs = {};

export function getCloseCompetitionInstructionDataEncoder(): FixedSizeEncoder<CloseCompetitionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 1)]]),
    (value) => ({ ...value, discriminator: CLOSE_COMPETITION_DISCRIMINATOR }),
  );
}

export function getCloseCompetitionInstructionDataDecoder(): FixedSizeDecoder<CloseCompetitionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 1)],
  ]);
}

export function getCloseCompetitionInstructionDataCodec(): FixedSizeCodec<
  CloseCompetitionInstructionDataArgs,
  CloseCompetitionInstructionData
> {
  return combineCodec(
    getCloseCompetitionInstructionDataEncoder(),
    getCloseCompetitionInstructionDataDecoder(),
  );
}

export type CloseCompetitionInput<
  TAccountCompetition extends string = string,
  TAccountCrank extends string = string,
  TAccountPermission extends string = string,
  TAccountMagicContext extends string = string,
  TAccountMagicProgram extends string = string,
  TAccountProtocolConfig extends string = string,
> = {
  /** CompetitionState PDA — currently delegated to PER */
  competition: Address<TAccountCompetition>;
  /** Protocol crank keypair — must equal config.crank_authority */
  crank: TransactionSigner<TAccountCrank>;
  /** MagicBlock ACL permission PDA — undelegated alongside competition */
  permission: Address<TAccountPermission>;
  /** MagicBlock context account for commit_and_undelegate_accounts CPI */
  magicContext: Address<TAccountMagicContext>;
  /** MagicBlock program */
  magicProgram: Address<TAccountMagicProgram>;
  /** ProtocolConfig PDA (read-only) — provides crank_authority */
  protocolConfig: Address<TAccountProtocolConfig>;
};

export function getCloseCompetitionInstruction<
  TAccountCompetition extends string,
  TAccountCrank extends string,
  TAccountPermission extends string,
  TAccountMagicContext extends string,
  TAccountMagicProgram extends string,
  TAccountProtocolConfig extends string,
  TProgramAddress extends Address = typeof TYCHE_CORE_PROGRAM_ADDRESS,
>(
  input: CloseCompetitionInput<
    TAccountCompetition,
    TAccountCrank,
    TAccountPermission,
    TAccountMagicContext,
    TAccountMagicProgram,
    TAccountProtocolConfig
  >,
  config?: { programAddress?: TProgramAddress },
): CloseCompetitionInstruction<
  TProgramAddress,
  TAccountCompetition,
  TAccountCrank,
  TAccountPermission,
  TAccountMagicContext,
  TAccountMagicProgram,
  TAccountProtocolConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? TYCHE_CORE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    competition: { value: input.competition ?? null, isWritable: true },
    crank: { value: input.crank ?? null, isWritable: false },
    permission: { value: input.permission ?? null, isWritable: true },
    magicContext: { value: input.magicContext ?? null, isWritable: true },
    magicProgram: { value: input.magicProgram ?? null, isWritable: false },
    protocolConfig: { value: input.protocolConfig ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("competition", accounts.competition),
      getAccountMeta("crank", accounts.crank),
      getAccountMeta("permission", accounts.permission),
      getAccountMeta("magicContext", accounts.magicContext),
      getAccountMeta("magicProgram", accounts.magicProgram),
      getAccountMeta("protocolConfig", accounts.protocolConfig),
    ],
    data: getCloseCompetitionInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseCompetitionInstruction<
    TProgramAddress,
    TAccountCompetition,
    TAccountCrank,
    TAccountPermission,
    TAccountMagicContext,
    TAccountMagicProgram,
    TAccountProtocolConfig
  >);
}

export type ParsedCloseCompetitionInstruction<
  TProgram extends string = typeof TYCHE_CORE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** CompetitionState PDA — currently delegated to PER */
    competition: TAccountMetas[0];
    /** Protocol crank keypair — must equal config.crank_authority */
    crank: TAccountMetas[1];
    /** MagicBlock ACL permission PDA — undelegated alongside competition */
    permission: TAccountMetas[2];
    /** MagicBlock context account for commit_and_undelegate_accounts CPI */
    magicContext: TAccountMetas[3];
    /** MagicBlock program */
    magicProgram: TAccountMetas[4];
    /** ProtocolConfig PDA (read-only) — provides crank_authority */
    protocolConfig: TAccountMetas[5];
  };
  data: CloseCompetitionInstructionData;
};

export function parseCloseCompetitionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseCompetitionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      competition: getNextAccount(),
      crank: getNextAccount(),
      permission: getNextAccount(),
      magicContext: getNextAccount(),
      magicProgram: getNextAccount(),
      protocolConfig: getNextAccount(),
    },
    data: getCloseCompetitionInstructionDataDecoder().decode(instruction.data),
  };
}
