/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TYCHE_AUCTION_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_BID_RECORD_DISCRIMINATOR = new Uint8Array([5]);

export function getCloseBidRecordDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 1).encode(
    CLOSE_BID_RECORD_DISCRIMINATOR,
  );
}

export type CloseBidRecordInstruction<
  TProgram extends string = typeof TYCHE_AUCTION_PROGRAM_ADDRESS,
  TAccountBidRecord extends string | AccountMeta<string> = string,
  TAccountCompetition extends string | AccountMeta<string> = string,
  TAccountBidder extends string | AccountMeta<string> = string,
  TAccountCallerProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountBidRecord extends string
        ? WritableAccount<TAccountBidRecord>
        : TAccountBidRecord,
      TAccountCompetition extends string
        ? ReadonlyAccount<TAccountCompetition>
        : TAccountCompetition,
      TAccountBidder extends string
        ? WritableSignerAccount<TAccountBidder> &
            AccountSignerMeta<TAccountBidder>
        : TAccountBidder,
      TAccountCallerProgram extends string
        ? ReadonlySignerAccount<TAccountCallerProgram> &
            AccountSignerMeta<TAccountCallerProgram>
        : TAccountCallerProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CloseBidRecordInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseBidRecordInstructionDataArgs = {};

export function getCloseBidRecordInstructionDataEncoder(): FixedSizeEncoder<CloseBidRecordInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 1)]]),
    (value) => ({ ...value, discriminator: CLOSE_BID_RECORD_DISCRIMINATOR }),
  );
}

export function getCloseBidRecordInstructionDataDecoder(): FixedSizeDecoder<CloseBidRecordInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 1)],
  ]);
}

export function getCloseBidRecordInstructionDataCodec(): FixedSizeCodec<
  CloseBidRecordInstructionDataArgs,
  CloseBidRecordInstructionData
> {
  return combineCodec(
    getCloseBidRecordInstructionDataEncoder(),
    getCloseBidRecordInstructionDataDecoder(),
  );
}

export type CloseBidRecordInput<
  TAccountBidRecord extends string = string,
  TAccountCompetition extends string = string,
  TAccountBidder extends string = string,
  TAccountCallerProgram extends string = string,
> = {
  /** BidRecord PDA */
  bidRecord: Address<TAccountBidRecord>;
  /** CompetitionState PDA */
  competition: Address<TAccountCompetition>;
  /** Bidder */
  bidder: TransactionSigner<TAccountBidder>;
  /** escrow program CPI caller */
  callerProgram: TransactionSigner<TAccountCallerProgram>;
};

export function getCloseBidRecordInstruction<
  TAccountBidRecord extends string,
  TAccountCompetition extends string,
  TAccountBidder extends string,
  TAccountCallerProgram extends string,
  TProgramAddress extends Address = typeof TYCHE_AUCTION_PROGRAM_ADDRESS,
>(
  input: CloseBidRecordInput<
    TAccountBidRecord,
    TAccountCompetition,
    TAccountBidder,
    TAccountCallerProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CloseBidRecordInstruction<
  TProgramAddress,
  TAccountBidRecord,
  TAccountCompetition,
  TAccountBidder,
  TAccountCallerProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TYCHE_AUCTION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    bidRecord: { value: input.bidRecord ?? null, isWritable: true },
    competition: { value: input.competition ?? null, isWritable: false },
    bidder: { value: input.bidder ?? null, isWritable: true },
    callerProgram: { value: input.callerProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("bidRecord", accounts.bidRecord),
      getAccountMeta("competition", accounts.competition),
      getAccountMeta("bidder", accounts.bidder),
      getAccountMeta("callerProgram", accounts.callerProgram),
    ],
    data: getCloseBidRecordInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseBidRecordInstruction<
    TProgramAddress,
    TAccountBidRecord,
    TAccountCompetition,
    TAccountBidder,
    TAccountCallerProgram
  >);
}

export type ParsedCloseBidRecordInstruction<
  TProgram extends string = typeof TYCHE_AUCTION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** BidRecord PDA */
    bidRecord: TAccountMetas[0];
    /** CompetitionState PDA */
    competition: TAccountMetas[1];
    /** Bidder */
    bidder: TAccountMetas[2];
    /** escrow program CPI caller */
    callerProgram: TAccountMetas[3];
  };
  data: CloseBidRecordInstructionData;
};

export function parseCloseBidRecordInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseBidRecordInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      bidRecord: getNextAccount(),
      competition: getNextAccount(),
      bidder: getNextAccount(),
      callerProgram: getNextAccount(),
    },
    data: getCloseBidRecordInstructionDataDecoder().decode(instruction.data),
  };
}
