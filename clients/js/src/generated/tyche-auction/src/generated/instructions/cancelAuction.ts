/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TYCHE_AUCTION_PROGRAM_ADDRESS } from "../programs";

export const CANCEL_AUCTION_DISCRIMINATOR = new Uint8Array([4]);

export function getCancelAuctionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 1).encode(
    CANCEL_AUCTION_DISCRIMINATOR,
  );
}

export type CancelAuctionInstruction<
  TProgram extends string = typeof TYCHE_AUCTION_PROGRAM_ADDRESS,
  TAccountAuctionState extends string | AccountMeta<string> = string,
  TAccountCompetition extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountRentRecipient extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuctionState extends string
        ? WritableAccount<TAccountAuctionState>
        : TAccountAuctionState,
      TAccountCompetition extends string
        ? ReadonlyAccount<TAccountCompetition>
        : TAccountCompetition,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountRentRecipient extends string
        ? WritableAccount<TAccountRentRecipient>
        : TAccountRentRecipient,
      ...TRemainingAccounts,
    ]
  >;

export type CancelAuctionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CancelAuctionInstructionDataArgs = {};

export function getCancelAuctionInstructionDataEncoder(): FixedSizeEncoder<CancelAuctionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 1)]]),
    (value) => ({ ...value, discriminator: CANCEL_AUCTION_DISCRIMINATOR }),
  );
}

export function getCancelAuctionInstructionDataDecoder(): FixedSizeDecoder<CancelAuctionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 1)],
  ]);
}

export function getCancelAuctionInstructionDataCodec(): FixedSizeCodec<
  CancelAuctionInstructionDataArgs,
  CancelAuctionInstructionData
> {
  return combineCodec(
    getCancelAuctionInstructionDataEncoder(),
    getCancelAuctionInstructionDataDecoder(),
  );
}

export type CancelAuctionInput<
  TAccountAuctionState extends string = string,
  TAccountCompetition extends string = string,
  TAccountAuthority extends string = string,
  TAccountRentRecipient extends string = string,
> = {
  /** AuctionState PDA */
  auctionState: Address<TAccountAuctionState>;
  /** CompetitionState PDA */
  competition: Address<TAccountCompetition>;
  /** Competition authority */
  authority: TransactionSigner<TAccountAuthority>;
  /** Rent recipient */
  rentRecipient: Address<TAccountRentRecipient>;
};

export function getCancelAuctionInstruction<
  TAccountAuctionState extends string,
  TAccountCompetition extends string,
  TAccountAuthority extends string,
  TAccountRentRecipient extends string,
  TProgramAddress extends Address = typeof TYCHE_AUCTION_PROGRAM_ADDRESS,
>(
  input: CancelAuctionInput<
    TAccountAuctionState,
    TAccountCompetition,
    TAccountAuthority,
    TAccountRentRecipient
  >,
  config?: { programAddress?: TProgramAddress },
): CancelAuctionInstruction<
  TProgramAddress,
  TAccountAuctionState,
  TAccountCompetition,
  TAccountAuthority,
  TAccountRentRecipient
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TYCHE_AUCTION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    auctionState: { value: input.auctionState ?? null, isWritable: true },
    competition: { value: input.competition ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: false },
    rentRecipient: { value: input.rentRecipient ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("auctionState", accounts.auctionState),
      getAccountMeta("competition", accounts.competition),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("rentRecipient", accounts.rentRecipient),
    ],
    data: getCancelAuctionInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelAuctionInstruction<
    TProgramAddress,
    TAccountAuctionState,
    TAccountCompetition,
    TAccountAuthority,
    TAccountRentRecipient
  >);
}

export type ParsedCancelAuctionInstruction<
  TProgram extends string = typeof TYCHE_AUCTION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** AuctionState PDA */
    auctionState: TAccountMetas[0];
    /** CompetitionState PDA */
    competition: TAccountMetas[1];
    /** Competition authority */
    authority: TAccountMetas[2];
    /** Rent recipient */
    rentRecipient: TAccountMetas[3];
  };
  data: CancelAuctionInstructionData;
};

export function parseCancelAuctionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelAuctionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      auctionState: getNextAccount(),
      competition: getNextAccount(),
      authority: getNextAccount(),
      rentRecipient: getNextAccount(),
    },
    data: getCancelAuctionInstructionDataDecoder().decode(instruction.data),
  };
}
