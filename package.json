{
  "name": "tyche-workspace",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "generate": "shank idl --crate-root programs/tyche-core --out-dir clients/idls && shank idl --crate-root programs/tyche-escrow --out-dir clients/idls && shank idl --crate-root programs/tyche-auction --out-dir clients/idls && ts-node scripts/fix-idls.ts && ts-node scripts/codama.ts && node scripts/patch-discriminators.cjs && node scripts/restore-pkg-names.cjs",
    "build:sdk": "npx tsup --config packages/sdk/tsup.config.ts",
    "typecheck:sdk": "npx tsc --project packages/sdk/tsconfig.json --noEmit",
    "clean:sdk": "node -e \"require('fs').rmSync('packages/sdk/dist', { recursive: true, force: true })\""
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "@codama/nodes-from-anchor": "^1.3.9",
    "@codama/renderers-js": "^2.0.3",
    "codama": "^1.5.1"
  }
}
